{
  "name": "hermes-glass",
  "version": "1.0.0",
  "description": "HermesGlass — Even Realities G2 app to talk with Hermes Agent",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "node -e \"require('node:fs').rmSync('dist-test',{recursive:true,force:true})\" && tsc -p tsconfig.test.json && node --test test/*.test.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.11"
  }
}
